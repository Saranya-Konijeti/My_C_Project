#include <stdio.h>

void main()
{
   int num,sum=0,i;
   printf("enter num");
   scanf("%d",&num);
   for(i=1;i<=num/2;i++){
      if(num%i==0)
      sum = sum+i;}
      if(sum==num)
      printf("perfect num");
      else
      printf("not a perfect number");
      
}
